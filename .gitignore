# Binary (root directory only)
/monitor
/monitor-bin
/migrate
/testenv
# Database
*.db
*.db-shm
*.db-wal
*.db.backup_*

# IDE
.idea/
.vscode/
/.claude/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test
coverage.out
coverage.html
*.test

# Air hot reload temporary files
tmp/

# Development environment PID and FIFO files
.dev/

# Logs
*.log

# Frontend build artifacts
frontend/node_modules/
frontend/dist/
frontend/.env*
!frontend/.env.production  # 允许提交生产环境配置模板

# Embedded frontend (symlink for development)
internal/api/frontend/

# monitors.d/ runtime data (generated by onboarding/admin, contains API keys)
monitors.d/

# Config files
config.yaml
config.local.yaml
config.production.yaml  # 生产配置包含真实数据，不提交
prd.config.yaml

# Environment variables
.env
.env.local
.env.*
!/.env.example
!/.env.pg.example
# Monitor data directory (root level only, avoid matching internal/monitor/)
/monitor/
!/monitor/.gitkeep

# Notifier development files
notifier/data/
notifier/tmp/
notifier/config.local.yaml
notifier/notifier

# Tools
.playwright-mcp/
config.yaml.backup-*
verify
notes.txt

# Go cache
.gocache/
/server

.bak
# 归档输出文件（运行时生成，不应提交）
archive/probe_history_*.csv
archive/probe_history_*.csv.gz
# genconfig binary (root level only, avoid matching cmd/genconfig/)
/genconfig
request-body.txt
