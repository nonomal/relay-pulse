# ============================================
# RelayPulse 本地 PostgreSQL 开发环境配置
# ============================================
# 此文件仅包含本地开发所需的数据库连接配置
# 不包含敏感的 API Keys，可安全提交到版本控制
#
# 使用方式：make dev-pg

# 时区设置
TZ=Asia/Shanghai

# Gin 运行模式
GIN_MODE=debug

# ============================================
# 存储配置（本地 PostgreSQL）
# ============================================
MONITOR_STORAGE_TYPE=postgres

# 本地 PostgreSQL 连接信息
# 容器名：relay-pulse-pg，端口映射 5433:5432
MONITOR_POSTGRES_HOST=localhost
MONITOR_POSTGRES_PORT=5433
MONITOR_POSTGRES_USER=monitor
MONITOR_POSTGRES_PASSWORD=monitor123
MONITOR_POSTGRES_DATABASE=llm_monitor
MONITOR_POSTGRES_SSLMODE=disable

# PostgreSQL 连接池配置
MONITOR_POSTGRES_MAX_OPEN_CONNS=25
MONITOR_POSTGRES_MAX_IDLE_CONNS=5
MONITOR_POSTGRES_CONN_MAX_LIFETIME=1h

# ============================================
# CORS 配置
# ============================================
# 注意：CORS 由 Makefile 的 MONITOR_CORS_ORIGINS 变量控制
# 此处不设置，避免配置冲突

# ============================================
# 注意事项
# ============================================
# 1. API Keys 未配置，探测功能会返回认证错误（预期行为）
# 2. 如需测试真实 API，请使用 make dev-pg-prod（会加载生产 Keys）
# 3. 本地 PG 容器启动命令：make pg-up
# 4. 恢复生产数据：make pg-restore
